# Importing secret properties config
spring.config.import=optional:file:./src/main/resources/application.secrets.properties

# Database config
spring.datasource.url=jdbc:postgresql://localhost:5432/identity?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true?createDatabaseIfNotExist=true
spring.datasource.username=postgres
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.generate_statistics=true

# Application config
spring.application.name=identity-service

# Server config
server.error.include-stacktrace=never
server.port=0

# Eureka config
eureka.instance.instance-id=${spring.application.name}:${random.int}

# Docs config
springdoc.swagger-ui.path=/docs

# Api config
api.security.token.jwt.secret=secret
api.security.provider=identity-service
api.email=victorkayk77@gmail.com

# RabbitMQ config
spring.rabbitmq.queue.notification=email-notification-queue
spring.rabbitmq.exchange.notification=email-notification-exchange
spring.rabbitmq.routing-key.notification=email-notification-routing-key

spring.rabbitmq.addresses=amqps://rqonxspc:KZeFMeo_nBm9HitPIp7cmWeP9aIjhsSu@prawn.rmq.cloudamqp.com/rqonxspc

# Actuator config
management.endpoints.web.exposure.include=*